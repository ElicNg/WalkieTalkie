<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity">

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Record"
        android:id="@+id/btnRecord"
        android:layout_alignBaseline="@+id/btnPlayLatest"
        android:layout_alignBottom="@+id/btnPlayLatest"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_marginLeft="80dp"
        android:layout_marginStart="80dp" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Play Latest"
        android:id="@+id/btnPlayLatest"
        android:layout_marginLeft="28dp"
        android:layout_marginStart="28dp"
        android:onClick="btnPlayLatest_onClick"
        android:layout_alignParentBottom="true"
        android:layout_toRightOf="@+id/btnRecord"
        android:layout_toEndOf="@+id/btnRecord" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Open debug "
        android:id="@+id/btnOpenAudioRecorderBufferActivity"
        android:layout_alignParentTop="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:onClick="btnOpenAudioRecorderBufferActivity_onClick" />

</RelativeLayout>
